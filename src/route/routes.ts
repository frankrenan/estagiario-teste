import { Router } from "express";

/* Deve ser declarado aqui os métodos GET / POST / PUT / DELETE */

const router = Router();

router.get("", ()=>{});
router.put("", ()=>{});
router.delete("", ()=>{});
router.post("", ()=>{});

export { router };

